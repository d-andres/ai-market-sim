# ── Core world / state modelling ──────────────────────────────────────────────
pydantic>=2.0,<3.0

# ── Async runtime ─────────────────────────────────────────────────────────────
# asyncio is part of the stdlib; no extra install needed.

# ── Web API + ASGI server ─────────────────────────────────────────────────────
# FastAPI exposes simulation state over HTTP/WebSocket; uvicorn runs the server.
fastapi>=0.110
uvicorn[standard]>=0.29

# ── UI Framework ──────────────────────────────────────────────────────────────
# NiceGUI provides the web interface for real-time visualization.
nicegui>=1.4

# ── Agentic framework (LLM decision-making) ───────────────────────────────────
# [litellm] extra bundles LiteLLM, which handles all provider routing (Ollama, OpenAI, etc.)
smolagents[litellm]>=1.0
